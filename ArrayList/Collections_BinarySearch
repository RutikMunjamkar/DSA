public static void main(String[] args) {
        List<Integer> list = Arrays.asList(1, 3, 6, 8, 10, 12);
        System.out.println(Collections.binarySearch(list,1111));
        // if it is found then the index is returned
       //else the -(inserationpoint)-1 is retunred 
        // return = -(insertionPoint) - 1
    }
